{
  "name": "basic-banking-system",
  "version": "1.0.0",
  "description": "A simple banking system with Node.js and MongoDB.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-frontend": "echo 'Build frontend if using build tools' && exit 0",
    "start-frontend": "echo 'Serve frontend files if needed' && exit 0",
    "dev:all": "concurrently \"npm run dev\" \"npm run start-frontend\""
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "mongodb": "^5.5.0",
    "mongoose": "^8.5.2"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.22"
  }
}
